{{ form_start(form) }}

 {#   {{ form_widget(form) }}        #}

{{ form_row(form.nome) }}

{{ form_row(form.diaria) }}

{{ form_row(form.tipo) }}


<div class="form-group">
    {{ form_label(form.descricao) }}
    {{ form_widget(form.descricao) }}
    {{ form_errors(form.descricao) }}

</div>


    <a name="botoes" href="{{ path('quarto_index') }}"  class="btn btn-info">Salvar</a>

{{ form_end(form) }}
